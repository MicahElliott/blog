{:title "Keyboard Remapping for Lisp"
 :layout :post
 :tags  ["keyboard" "lisp" "draft"]
}

Do you find that `(` and `)` are a pain to constantly reach for? You may have
already swapped your _Ctrl_ and _CapsLock_ keys, but in the post I’ll show you a
sensible way to take it to the next level. Rearranging a few important keys can
really improve your comfort level and productivity.

++++
<!-- more -->
++++

I keep a personal script file that takes care of all my mappings:
`~/bin/modmap`.

== How `xmodmap` works
